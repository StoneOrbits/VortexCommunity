<% if ((isDuplicate & 1) === 0) { %>
    <div class="pat-item-submission <%= (isDuplicate & 2) ? 'duplicate' : '' %>" data-index="<%= tempId %>" data-pattern-id="<%= pat._id %>">
        <div class="pat-item-submission-content">
            <input type="hidden" name="patIds[]" value="<%= tempId %>" <%= (isDuplicate & 2) ? 'disabled' : '' %> />
            <p class="pat-name">
              <%= (isDuplicate & 2) && duplicateName ? duplicateName : patName %>
            </p>
        </div>
        <!-- Lightshow visualization -->
        <%- include('submission-lightshow', { pat: pat, tempId: tempId }) %>
    </div>
<% } %>
<input type="hidden" name="patternNames[]" id="patternName-<%= tempId %>" value="<%= patName || '' %>">
<input type="hidden" name="patternDescriptions[]" id="patternDescription-<%= tempId %>" value="<%= patDescription || '' %>">
