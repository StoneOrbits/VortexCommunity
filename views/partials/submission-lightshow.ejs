<div class="submission-led-strip-container">
    <canvas id="lightshowCanvas_<%= typeof tempId !== 'undefined' ? tempId : pat._id %>" class="lightshow-canvas" data-pat='<%= JSON.stringify(pat.data ? pat.data : pat) %>'></canvas>
</div>
